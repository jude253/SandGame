{
    "version": 3,
    "configurePresets": [
        {
            "name": "default",
            "description": "Default settings for my cmake project",
            "hidden": false,
            "generator": "Unix Makefiles",
            "binaryDir": "${workspaceFolder}/build/default",
            "cacheVariables": {
                "USE_EMSCRIPTEN": false
            }
        },
        {
            "name": "wasm",
            "description": "Default settings for my cmake project",
            "hidden": false,
            "generator": "Unix Makefiles",
            "binaryDir": "${workspaceFolder}/build/wasm",
            "cacheVariables": {
                "USE_EMSCRIPTEN": true
            }
        }
    ]
}