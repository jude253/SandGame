#pragma once

#include <SDL.h>

typedef struct {
    Uint8 r;
    Uint8 g; 
    Uint8 b; 
    Uint8 a;
} Color;

extern Color RED;
extern Color DARK_RED;
extern Color BLUE;
extern Color GREEN;
extern Color WHITE;
extern Color BLACK;
extern Color BACKGROUND_COLOR;